import { TopChangeDirective } from './top-change.directive';

describe('TopChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new TopChangeDirective();
    expect(directive).toBeTruthy();
  });
});
